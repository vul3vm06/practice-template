#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
  vector<int> combine(int n, int k) {
    vector<int> sol(n);
    return sol;
  }
};

int main() {
  int n, k;
  cin >> n >> k;

  return 0;
}
